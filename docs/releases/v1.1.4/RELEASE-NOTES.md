# Release Notes

## v1.1.4 — Dynamic Version Injection

### What's New

- **Dynamic versioning** — NSIS installer now pulls version dynamically at build time, removing the need for manual version bumps
- Makefile and CI workflow updated to support the new version injection flow
